# Base URL of the orders API (see architecture in README).
VITE_API_BASE_URL=https://api.your-domain.com

# Set to "true" to run the UI against in-memory fixture data instead of a
# live API — useful for frontend development before the backend exists.
VITE_USE_MOCK_API=false
